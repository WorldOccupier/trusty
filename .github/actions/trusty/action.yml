name: 'Trusty AI Code Verification'
description: 'Verify AI-generated code with static analysis, semantic checks, and behavioral verification'
author: 'Trusty'

inputs:
  min-score:
    description: 'Minimum trust score (0-100)'
    required: false
    default: '70'
  format:
    description: 'Output format (json, sarif)'
    required: false
    default: 'sarif'
  token:
    description: 'GitHub token for PR comments'
    required: false
    default: ${{ github.token }}
  openai-api-key:
    description: 'OpenAI API key for LLM-based analysis'
    required: false

outputs:
  trust-score:
    description: 'Overall trust score'
    value: ${{ steps.scan.outputs.trust-score }}
  status:
    description: 'Scan status (clean, warning, failed)'
    value: ${{ steps.scan.outputs.status }}

runs:
  using: 'composite'
  steps:
    - name: Install Trusty
      shell: bash
      run: |
        if ! command -v trusty &> /dev/null; then
          echo "Installing Trusty..."
          go install github.com/WorldOccupier/trusty/cmd/trusty@latest
        fi

    - name: Run Trusty Scan
      id: scan
      shell: bash
      env:
        OPENAI_API_KEY: ${{ inputs.openai-api-key }}
      run: |
        trusty scan \
          --base origin/${{ github.base_ref }} \
          --head HEAD \
          --min-score ${{ inputs.min-score }} \
          --format ${{ inputs.format }} \
          --config .trusty.yml \
          > trusty-results.${{ inputs.format == 'sarif' && 'sarif' || 'json' }}

        SCORE=$(trusty scan \
          --base origin/${{ github.base_ref }} \
          --head HEAD \
          --min-score ${{ inputs.min-score }} \
          --format json 2>/dev/null | jq -r '.trust_score // 0')

        echo "trust-score=$SCORE" >> $GITHUB_OUTPUT

        if [ "$SCORE" -lt "${{ inputs.min-score }}" ]; then
          echo "status=failed" >> $GITHUB_OUTPUT
          echo "❌ Trust score $SCORE/${{ inputs.min-score }} — below minimum threshold"
          exit 1
        else
          echo "status=clean" >> $GITHUB_OUTPUT
          echo "✅ Trust score $SCORE"
        fi

    - name: Upload SARIF Results
      if: inputs.format == 'sarif'
      uses: github/codeql-action/upload-sarif@v3
      with:
        sarif_file: trusty-results.sarif
        category: trusty
